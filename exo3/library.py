# coding: utf8
from books import*
from books_list import*

class Library():
    """"""
    def __init__(self,):
        self.books_all = []
        self.title = title

    def add_book(self, book):
        self.books_all.append(book)
        """fonction pour ajouter un objet livre dans la liste book"""


    def get_book(self, title):
        for i in self.books_all:
            if title == books.title:
                return books.title
                print("oui")
